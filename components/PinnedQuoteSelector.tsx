// This component is no longer in use.
// The application has been updated to use a local `quotes-config.json` file
// to handle pinning a specific quote, which removes the need for this UI selector.
